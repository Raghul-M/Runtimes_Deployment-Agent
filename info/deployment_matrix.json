
[
  {
    "model_name": "gpt-oss-120b",
    "deployable": false,
    "reason": "Insufficient GPU capacity. The model requires 264 GB of VRAM (~6 GPUs), but the cluster only provides 135 GB (3 GPUs)."
  },
  {
    "model_name": "granite-3.1-8b-instruct",
    "deployable": true,
    "reason": "Model fits on a single GPU. However, the configuration is missing serving_arguments, which must be added for a successful deployment. See the recommended OPTIMIZED_SERVING_ARGUMENTS_JSON."
  }
]
